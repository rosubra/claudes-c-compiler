Refactor global_init.rs: Break up the 659-line lower_global_init() god function

Status: in_progress

Task:
- Break the massive lower_global_init() function into smaller, focused methods
- Deduplicate the 3x copy-pasted string literal handling (narrow/wide/char16)
- Deduplicate the vector compound literal handling (copy-pasted for cast-unwrapped case)
- Extract the duplicated overwrite-or-extend pattern in flatten_global_array_init_bool
- Reduce deep nesting (8+ levels in some places)
- Make the code easier to understand and maintain

Files affected:
- src/ir/lowering/global_init.rs (primary)
