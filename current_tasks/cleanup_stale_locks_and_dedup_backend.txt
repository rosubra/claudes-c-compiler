Status: In progress

Task: Clean up stale completed task locks and deduplicate backend codegen

1. Remove 8 stale task lock files (completed tasks left behind):
   - fix_complex_abi_function_passing.txt
   - fix_funclike_macro_rescan_chaining.txt
   - fix_has_builtin_and_glibc_compat.txt
   - fix_if_directive_include_pending.txt
   - fix_lowering_scope_clone_perf.txt
   - fix_token_paste_chain_whitespace.txt
   - implement_wide_string_literals.txt
   - fix_struct_layout_perf.txt

2. Deduplicate backend codegen by extracting shared patterns:
   - Add arch instruction table (branch/trap/test mnemonics) to ArchCodegen trait
   - Move simple terminators (emit_branch, emit_cond_branch, emit_unreachable,
     emit_indirect_branch) to default trait implementations
   - Move emit_label_addr to default trait implementation
   - Extract shared emit_gep logic
   - Provide default implementations for emit_load_operand, emit_store_result
   - Add README.md to backend/ subdirectories
