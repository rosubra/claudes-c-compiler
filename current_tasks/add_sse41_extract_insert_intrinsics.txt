Task: Add SSE4.1 extract/insert intrinsics (_mm_extract_epi32, _mm_insert_epi32, etc.)

Problem: libtomcrypt project fails to link because _mm_extract_epi32 and _mm_insert_epi32
are undefined. The smmintrin.h header only has CRC32 intrinsics but is missing SSE4.1
insert/extract operations.

Plan:
1. Add BuiltinIntrinsic variants for pinsrd/pextrd (32-bit insert/extract)
2. Register _mm_extract_epi32, _mm_insert_epi32, and __builtin_ia32_pextrd128, __builtin_ia32_pinsrd128
3. Add IntrinsicOp variants
4. Wire up IR lowering (extract = Scalar, insert = Vec128)
5. Implement x86 and i686 backend codegen (PINSRD/PEXTRD instructions)
6. Add ARM/RISC-V emulation
7. Update smmintrin.h with the intrinsic wrapper functions
8. Test with libtomcrypt project
