Task: Fix brace-wrapped string initializer for global/static char arrays, and improve global init

Problem 1: static char c[] = {"hello"} produces empty string
- Initializer::List with single StringLiteral item not handled in lower_global_init
- compute_init_list_array_size returns 1 instead of string_len+1 for this pattern
- Need to detect brace-wrapped string in both size computation and global init lowering

Problem 2: Various global initialization improvements needed
- Multi-dimensional float arrays
- Improve struct init with nested initializers

Fix plan:
1. In lower_global_init: detect brace-wrapped string literal and emit GlobalInit::String
2. In compute_init_list_array_size: detect single string literal and return string_len+1
3. Same fixes for both global declarations and static local declarations

Status: IN PROGRESS
